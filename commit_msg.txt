fix: game-code audit fixes (commission idempotency, ffc range, bet keys)

- CommissionService: add idemScope param; lottery passes lottery_{bet.id}
  (previously all lottery commissions after the first were silently
  swallowed by a constant idempotency key 'commission:flow:...')
- CommissionService: skip CommissionRecord creation when ledger credit
  was an idempotent replay, keeping records consistent with money
- ffc/ssc big/small range corrected to sum>=23 / <=22 (5-ball 0-45);
  previous 11-18/3-10 dice range made sums 19-45 lose both sides
- Lottery placeBet: create bet first, use bet.id in idempotency keys
  (Date.now() keys could collide on same-ms double tap = free bet)
- LotteryView: value picker now supports champion/runner (1-10) and
  kuai3 sum (4-17), resets to valid option on bet-type switch;
  history big/small flag uses per-game thresholds
- Lobby: slots-classic no longer mislabeled into lucky-wheel view
- Dashboard activeToday excludes platform account from BET grouping
- seed: ffc/ssc payTable desc updated to new range
