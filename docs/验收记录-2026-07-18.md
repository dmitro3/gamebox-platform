# 全量风险修复验收记录（2026-07-18）

## 环境

- Docker Desktop：`gamebox-postgres` / `gamebox-redis` 已启动
- 后端：`pnpm --filter @gamebox/server start:dev` → `http://localhost:3000`
- 迁移：`20260718010000_risk_foundation` 已 apply
- 种子：`prisma:seed` 已执行（管理员 `admin` / `Admin@123456`）

## 自动化结果

| 套件 | 结果 | 说明 |
|------|------|------|
| server Jest | 4 passed | fairness + table redis-state |
| client Vitest | 无用例 | 配置已就绪，暂无 `*.spec.ts` |
| client vue-tsc + vite build | 通过 | 生产 Dockerfile 已恢复 vue-tsc |
| server nest build | 通过 | |
| prisma validate | 通过 | |
| `scripts/smoke-acceptance.ps1` | 见下 | API 冒烟 |

### 冒烟脚本覆盖

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-acceptance.ps1
```

- health（DB + Redis）
- admin 登录
- 玩家注册（账号规则：字母+数字 8–20）
- 游戏目录（麻将 / 水果 / 龙虎）
- 充值申请 → 管理员审批入账
- 麻将 spin + `clientRequestId` 幂等
- 水果 spin
- 牌桌 snapshot（Redis 相位）
- 注单历史
- 余额与注单流水勾稽
- 玩家自审充值应被拒绝

## 计划项对照

| Todo | 状态 |
|------|------|
| foundation | 完成 |
| money-security | 完成（充提域/CAS/差额佣金等） |
| game-consistency | 完成 |
| mahjong-authority | 完成（服务端权威 + 客户端回放） |
| table-redis | 完成（leader / phaseEndsAt / DB CAS） |
| client-quality | 完成（去本地假结算、logout 清钱包、类型通过） |
| production-hardening | 完成（helmet/CORS/限流/health/部署幂等） |
| full-verification | 本记录 |

## 未覆盖 / 后续建议

1. **双实例牌桌 leader 选举**：需起两个 server 进程对接同一 Redis，手工或 compose scale 验证。
2. **client Vitest 业务用例**：当前 0 文件，建议补水果/麻将回放纯函数测试。
3. **并发扣款压测**：冒烟为串行；可用 k6/wrk 对 `/bet/mahjong/spin` 做并发透支验证。
4. **生产 compose 全链路**：本地未跑 `docker-compose.prod.images.yml` 镜像构建（需先 build/load 镜像）。
5. **E2E UI**：未跑 Playwright/浏览器；前端需另开 `pnpm --filter @gamebox/client-app dev` 目视联调。

## 本地常用命令

```bash
docker compose up -d
pnpm --filter @gamebox/server exec prisma migrate deploy
pnpm --filter @gamebox/server prisma:seed
pnpm --filter @gamebox/server start:dev
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-acceptance.ps1
```

> `scripts/` 仅保留：`deploy-on-server.sh`（生产部署）、`smoke-acceptance.ps1`（API 冒烟验收）。
